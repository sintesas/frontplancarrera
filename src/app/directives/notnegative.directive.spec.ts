import { NotnegativeDirective } from './notnegative.directive';

describe('NotnegativeDirective', () => {
  it('should create an instance', () => {
    const directive = new NotnegativeDirective();
    expect(directive).toBeTruthy();
  });
});
